version: "3.7"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    command: poetry run gunicorn main_hms.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/homeswapd
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
      - celery
      - celery-beat

  redis:
    image: redis
    ports:
      - "6379:6379"

  celery:
    build: .
    command: poetry runcelery -A main_hms worker -l info
    volumes:
      - .:/homeswapd
    env_file:
      - .env
    depends_on:
      - redis
      - postgres

  celery-beat:
    build: .
    command: poetry run celery -A main_hms beat -l info
    volumes:
      - .:/homeswapd
    env_file:
      - .env
    depends_on:
      - redis
      - postgres

  flower:
    build: .
    command: poetry run celery -A main_hms flower --port=5555
    ports:
      - "5555:5555"
    volumes:
      - .:/homeswapd
    env_file:
      - .env
    depends_on:
      - redis
      - postgres
      - celery

  postgres:
    image: postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
